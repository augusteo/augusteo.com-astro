---
interface Props {
  href?: string;
  variant?: "sky" | "sage" | "terracotta";
  type?: "button" | "submit" | "reset";
  class?: string;
}

const { href, variant = "sky", type, class: className } = Astro.props;
const Element = href ? "a" : "button";
---

<Element
  href={href}
  type={!href ? type : undefined}
  class:list={["btn-organic", variant, className]}
>
  <slot />
</Element>
